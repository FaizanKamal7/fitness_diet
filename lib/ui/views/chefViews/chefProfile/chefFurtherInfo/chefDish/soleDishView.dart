import 'package:flutter/material.dart';

class SoleDishView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Sole Dish view"),
      ),
    );
  }
}
